<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.pjt.model.dao.UserDao">

    <!-- 로그인 -->
    <select id="login" resultType="com.ssafy.pjt.model.dto.User">
        SELECT * FROM user
        WHERE user_id = #{userId} AND password = #{password}
    </select>
    <select id="select" parameterType="string" resultType="com.ssafy.pjt.model.dto.User">
        SELECT * FROM user WHERE user_id = #{userId}
    </select>

    <!-- 회원가입 -->
    <insert id="insert" parameterType="com.ssafy.pjt.model.dto.User">
        INSERT INTO user (user_id, user_name, password, email, age)
        VALUES (#{userId}, #{userName}, #{password}, #{email}, #{age})
    </insert>

    <!-- 회원정보 수정 -->
    <update id="update" parameterType="com.ssafy.pjt.model.dto.User">
        UPDATE user
        SET user_name = #{userName},
            password = #{password},
            email = #{email},
            age = #{age}
        WHERE user_id = #{userId}
    </update>

    <!-- 탈퇴 -->
    <delete id="delete" parameterType="string">
        DELETE FROM user
        WHERE user_id = #{userId}
    </delete>

    <update id="updatePassword" parameterType="com.ssafy.pjt.model.dto.User">
        UPDATE user
        SET password = #{password}
        WHERE user_id = #{userId}
    </update>

</mapper>
